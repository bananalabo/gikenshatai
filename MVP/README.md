# MVP HTML Prototype

これは `Documentation/Wireframes/` を元にした、開発前提の静的HTMLプロトタイプです（バックエンドなし）。

## 使い方
1. ブラウザで `MVP/index.html` を開く
2. ナビゲーションで各画面（管理者/利用者/案件詳細（Admin/User共通）/プレビュー等）を確認する

※添付ファイルはサンプル表示です（`<input type="file">` は単にUIとして存在します）。

